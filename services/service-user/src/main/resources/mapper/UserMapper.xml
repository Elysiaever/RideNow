<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 命名空间必须与 Mapper 接口全路径一致 -->
<mapper namespace="com.fth.user.mapper.UserMapper">

    <!-- BaseResultMap：精准映射 User 实体类字段（与数据库 t_user 表字段对应） -->
    <resultMap id="BaseResultMap" type="com.fth.user.domain.model.User">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
        <result column="nickname" property="nickname" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="avatar" property="avatar" jdbcType="VARCHAR"/>
        <result column="gender" property="gender" jdbcType="VARCHAR"/>
        <result column="birthday" property="birthday" jdbcType="DATE"/>
    </resultMap>

    <!-- 公共字段列表：复用查询/插入字段 -->
    <sql id="Base_Column_List">
        id, username, password, phone, created_at, updated_at, nickname, email, avatar, gender, birthday
    </sql>

    <!-- 1. 根据用户名查询用户（返回 Optional<User>） -->
    <!-- 注意：MyBatis 会自动将 null 结果包装为 Optional.empty()，非 null 包装为 Optional.of(实体) -->
    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_user
        WHERE username = #{username, jdbcType=VARCHAR}
        <!-- 限制只返回1条（用户名唯一约束，避免多结果异常） -->
        LIMIT 1
    </select>

    <!-- 2. 统计用户名重复数（注册校验：返回>0则已存在） -->
    <select id="countByUsername" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM t_user
        WHERE username = #{username, jdbcType=VARCHAR}
    </select>

    <!-- 3. 统计手机号重复数（注册校验：返回>0则已存在） -->
    <select id="countByPhone" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM t_user
        WHERE phone = #{phone, jdbcType=VARCHAR}
    </select>


</mapper>